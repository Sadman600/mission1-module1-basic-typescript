// ** Function

// Normal Function
function myFunction(num1: number, num2: number): number {
  return num1 + num2;
}

// Arrow Function

const myArrowFunction = (num1: number, num2: number): number => {
  return num1 + num2;
};

const myArr: number[] = [5, 6, 9];
const arrOutput: number[] = myArr.map((value: number): number => value * value);
